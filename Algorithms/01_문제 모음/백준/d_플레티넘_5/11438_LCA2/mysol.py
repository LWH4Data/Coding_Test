import sys, time
sys.stdin = open("input.txt")
s_t = time.time()

# 노드의 수(N)와 간선의 수(M)가 크기 때문에 빠른 LCA를 사용해야 한다.
#================================================================

#================================================================

e_t = time.time()
print("time: ", e_t - s_t)