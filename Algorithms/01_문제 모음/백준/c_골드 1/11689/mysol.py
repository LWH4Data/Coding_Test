import sys, time
sys.stdin = open("input.txt")
start = time.time()

#=========================================
# 입력값 받기
n = int(sys.stdin.readline())


# 오일러 피 함수를 구현하고

# 주어진 깂 n의 인덱스 위치의 값을 출력하면 된다.
#=========================================

end = time.time()
print("time: ", end - start)